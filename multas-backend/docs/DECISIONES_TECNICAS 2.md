# Decisiones técnicas históricas

Este archivo corresponde al diseño inicial y queda reemplazado por `DECISIONES_TECNICAS.md`.

La decisión vigente es React/Vite → Express `/api/v1` → MySQL, autenticación propia Argon2id con cookie HttpOnly, TANDA 0 terminada y TANDA 2 implementada mediante la migración 004.
