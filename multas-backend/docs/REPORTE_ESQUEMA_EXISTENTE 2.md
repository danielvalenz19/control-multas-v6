# Reporte histórico del esquema

Este archivo conserva la referencia al primer diagnóstico y queda reemplazado por `REPORTE_ESQUEMA_EXISTENTE.md`.

Estado vigente: contrato auth coincidente; migración 004 aplicada; administrador real activo; 64/64 permisos ADMIN; 2 sesiones revocadas y 0 activas. El contrato completo solo mantiene pendientes `infractions`, `payments` y `solvencies`.
